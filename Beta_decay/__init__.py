"""Legacy beta-decay QRPA emulator workflows.

These modules are kept importable while the reusable SMLR package API is
assembled under ``src/smlr``. Run the training entry points with
``python -m Beta_decay.main`` or ``python -m Beta_decay.main_only_HL``.
"""
